<div class="container">
  <div class="tracks-form">
  <h1>Submit your part</h1>
  <h2 style="font-size: 19px !important">
    To collaborate with
    <%= @request.track.user.username %>
    on
    <%= @request.track.name %>
  </h2>
      <p class="mt-4"></p>
      <%= simple_form_for ([@request, @submission]) do |s| %>
        <%= s.input :description, label: 'Short description of your collaboration'%>
        <!-- <div class="row">
          <div class="col-6">
            <%= s.input :bpm %>
          </div>
          <div class="col-6">
            <%= s.input :time_signature %>
          </div>
        </div>
        <div class="row">
          <div class="col-6">
            <%= s.input :key %>
          </div>
          <div class="col-6">
            <%= s.input :duration %>
          </div>
        </div> -->
        <%= s.input :file %>
        <%= s.submit "Collaborate", class: "btn btn-primary"%>
      <% end %>
    </div>
</div>
